#!/usr/bin/node
class Rectangle {
    // class attributes
    w;
    h;

    // initialize an instance with these values
    constructor (w, h) {
        this.width = w;
        this.height = h;
    }
}
